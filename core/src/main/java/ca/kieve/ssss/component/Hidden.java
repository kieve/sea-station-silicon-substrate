package ca.kieve.ssss.component;

/**
 * Marker component that prevents an entity from being rendered.
 * Used to hide the player sprite while socketed into a body.
 */
public record Hidden() implements Component {}
