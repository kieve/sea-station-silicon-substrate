- Confirm modals are kind of ass. Fix the styling
  - save on exit
  - removing blocks that already exist in the map

Don't pass ContentRegistry and *Resolvers through parameters.
Define a static singleton way with a "getInstance()" to get references. Then have 
